<!-- METADATA: {"source_path": "agent/engine.py", "source_sha": "", "extraction_quality": "full_ast", "model": "gpt-5-mini", "generated_at": "2026-06-04T22:21:40Z", "doc_type": "file"} -->
<details>
<summary>Documentation Metadata (click to expand)</summary>

```json
{
  "doc_type": "file_overview",
  "file_path": "agent/engine.py",
  "source_hash": "4ae7a352be4242af8a909df89e37cf22b549e91b16834765193b6b54fe542f71",
  "last_updated": "2026-06-04T22:21:40.366501+00:00",
  "tokens_used": 11592,
  "complexity_score": 9,
  "estimated_review_time_minutes": 54,
  "external_dependencies": [
    "__future__",
    "concurrent.futures"
  ]
}
```

</details>

[Documentation Home](../README.md) > [agent](./README.md) > **engine**

---

# engine.py

> **File:** `agent/engine.py`

![Complexity: High](https://img.shields.io/badge/Complexity-High-red) ![Review Time: 54min](https://img.shields.io/badge/Review_Time-54min-blue)

## 📑 Table of Contents


- [Overview](#overview)
- [Dependencies](#dependencies)
- [Architecture Notes](#architecture-notes)
- [Maintenance Notes](#maintenance-notes)
- [Functions and Classes](#functions-and-classes)

---

## Overview

This module defines an engine that coordinates agent turns, summarization, and model selection logic. It provides dataclass representations for external context, brief turn summaries, and the main RLMEngine which likely orchestrates interaction with models and tools. Several small utility functions produce compact summaries of tool arguments and observations and determine model capability tiers to select appropriate executors.

 The file ties together model- and prompt-related imports with a ThreadPoolExecutor for concurrent work, suggesting it manages running model calls or tool executions and recording lightweight summaries of completed turns for session continuity. Helper functions encapsulate simple heuristics for summarizing inputs and choosing models by tier.

## Dependencies

### External Dependencies

| Module | Usage |
| --- | --- |
| `__future__` | from __future__ import annotations |
| `concurrent.futures` | from concurrent.futures import ThreadPoolExecutor |

### Internal Dependencies

| Module | Usage |
| --- | --- |
| `json` | import json |
| `re` | import re |
| `time` | import time |
| `threading` | import threading |
| `datetime` | from datetime import datetime, timezone |
| `contextlib` | from contextlib import nullcontext |
| `dataclasses` | from dataclasses import dataclass, field |
| `pathlib` | from pathlib import Path |
| `typing` | from typing import Any, Callable |
| `config` | from .config import AgentConfig |
| `model` | from .model import BaseModel, ImageData, ModelError, ModelTurn, ToolCall, ToolResult |
| `prompts` | from .prompts import build_system_prompt, detect_role_from_objective, INVESTIGATIVE_ROLES |
| `replay_log` | from .replay_log import ReplayLogger |
| `tool_defs` | from .tool_defs import get_tool_definitions |
| `tools` | from .tools import WorkspaceTools |

## 📁 Directory

This file is part of the **agent** directory. View the [directory index](_docs/agent/README.md) to see all files in this module.

## Architecture Notes

- Extensive use of dataclasses to model engine state and summaries.
- Small focused helper functions for lightweight summarization and model selection.
- Integration points with model and prompt submodules (imports of BaseModel, ModelTurn, ToolCall, prompts utilities).
- Concurrency support indicated by ThreadPoolExecutor import for executing work in parallel or off the main thread.

## Maintenance Notes

- Represent external invocation context with ExternalContext dataclass for use during agent operations.
- Capture concise information about completed interactions via TurnSummary dataclass to support continuity.
- Provide the RLMEngine dataclass which coordinates agent behavior, model interaction, and likely execution flow.
- Summarize tool call arguments and observations using _summarize_args and _summarize_observation to create compact previews.
- Determine model capability tiers and pick an appropriate executor with _model_tier and _lowest_tier_model utilities.

---

## Navigation

**↑ Parent Directory:** [Go up](_docs/agent/README.md)

---

*This documentation was automatically generated by AI ([Woden DocBot](https://github.com/marketplace/ai-document-creator)) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*


---

## Functions and Classes


#### _summarize_args

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _summarize_args(args: dict[str, Any], max_len: int = 120) -> str:
```

### Description

Generate a concise, single-line summary of a tool call's arguments by converting the provided arguments dictionary into a short string representation, truncating or limiting output to the specified maximum length.

Generate a concise, single-line summary of a tool call's arguments by converting the provided arguments dictionary into a short string representation, truncating or limiting output to the specified maximum length.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `args` | `dict[str, Any]` | ✅ | Parameter args |
| `max_len` = `120` | `int` | ❌ | Parameter max_len |

### Returns

**Type:** `str`

Returns a str

### Complexity

Not analyzed

---



#### _summarize_observation

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _summarize_observation(text: str, max_len: int = 200) -> str:
```

### Description

Produce a short preview of an observation by returning its first line or a truncated version of the provided text.

Produce a short preview of an observation by returning its first line or a truncated version of the provided text. It takes the input observation text and limits its length to a brief summary suitable for display as a preview.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `text` | `str` | ✅ | Parameter text |
| `max_len` = `200` | `int` | ❌ | Parameter max_len |

### Returns

**Type:** `str`

Returns a str

### Complexity

Not analyzed

---



#### _model_tier

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _model_tier(model_name: str, reasoning_effort: str | None = None) -> int:
```

### Description

Map a model identifier (and optionally a reasoning_effort hint) to a numeric capability tier where lower numbers indicate higher capability.

Map a model identifier (and optionally a reasoning_effort hint) to a numeric capability tier where lower numbers indicate higher capability. It encodes specific mappings for Anthropic models by name and for OpenAI Codex-style models by reasoning_effort, and defaults unknown cases to tier 2.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `model_name` | `str` | ✅ | Parameter model_name |
| `reasoning_effort` = `None` | `str | None` | ❌ | Parameter reasoning_effort |

### Returns

**Type:** `int`

Returns a int

### Complexity

Not analyzed

---



#### _lowest_tier_model

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _lowest_tier_model(model_name: str) -> tuple[str, str | None]:
```

### Description

Determine the lowest-tier executor model name and its associated reasoning effort.

Determine the lowest-tier executor model name and its associated reasoning effort. For recognized Anthropic model names this function maps them to the corresponding haiku model and returns the reasoning_effort; for unknown models it returns the original model name with no downgrade.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `model_name` | `str` | ✅ | Parameter model_name |

### Returns

**Type:** `tuple[str, str | None]`

Returns a tuple[str, str | None]

### Complexity

Not analyzed

---



#### ExternalContext

![Type: Concrete](https://img.shields.io/badge/Type-Concrete-green) ![Methods: 2](https://img.shields.io/badge/Methods-2-blue) ![Attributes: 1](https://img.shields.io/badge/Attributes-1-blue)

### Description

ExternalContext is a lightweight dataclass that collects and manages external textual observations relevant to the engine.

Within agent/engine.py, ExternalContext serves as the component responsible for holding and summarizing external input or context that the engine can use, keeping observation management separated from core engine logic.

Responsibilities:
- Store a chronologically maintained list of external observations (observations: list[str]).
- Provide an add method to append new observation strings to the collection.
- Provide a summary method to produce a concise aggregation or representation of the stored observations.
- Act as a single place to accumulate external context that other engine components can query or include in processing.

### Attributes

| Attribute | Type | Visibility | Description |
| --- | --- | --- | --- |
| `observations` (class) | `list[str]` | 🔓 public | Class attribute observations<br>**Default:** `field(default_factory=list)` |

### Methods

#### Public Methods

| Method | Type | Description |
| --- | --- | --- |
| `add` | instance | Method add.<br>**Signature:** `def add(self, text: str) -> None:` |
| `summary` | instance | Method summary.<br>**Signature:** `def summary(self, max_items: int = 12, max_chars: int = 8000) -> str:` |

---



#### add

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def add(self, text: str) -> None:
```

### Description

Adds the given text into the ExternalContext object so it becomes part of the context managed by the engine.

Adds the given text into the ExternalContext object so it becomes part of the context managed by the engine. This method appends or incorporates the provided string into the external context storage for later use by other engine components.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `text` | `str` | ✅ | Parameter text |

### Complexity

Not analyzed

---



#### summary

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def summary(self, max_items: int = 12, max_chars: int = 8000) -> str:
```

### Description

Generate and return a condensed summary of the ExternalContext contents, constrained by limits on the number of items and total characters.

Generate and return a condensed summary of the ExternalContext contents, constrained by limits on the number of items and total characters. The method produces a human-readable string that represents the most relevant pieces of the external context up to the specified limits.


max_items controls the maximum number of context items to include in the summary (default 12) and max_chars bounds the total length of the returned string in characters (default 8000).

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `max_items` = `12` | `int` | ❌ | Parameter max_items |
| `max_chars` = `8000` | `int` | ❌ | Parameter max_chars |

### Returns

**Type:** `str`

Returns a str

### Complexity

Not analyzed

---



#### TurnSummary

![Type: Concrete](https://img.shields.io/badge/Type-Concrete-green) ![Methods: 2](https://img.shields.io/badge/Methods-2-blue) ![Attributes: 6](https://img.shields.io/badge/Attributes-6-blue)

### Description

TurnSummary is a lightweight dataclass that captures a concise snapshot of a completed agent turn, recording identifying metadata and a short preview of the result.

Within agent/engine.py, TurnSummary serves as the simple data container used by the engine to summarize completed turns for continuity and replay purposes. It enables other components to record, serialize, and restore recent turn-level state without carrying full execution context.

Responsibilities:
- Hold core metadata about a completed turn: turn_number, objective, result_preview, timestamp, steps_used, and replay_seq_start.
- Provide a compact, serializable representation of a turn so state can be persisted or transmitted.
- Offer conversion to a primitive dictionary form for storage or logging (to_dict).
- Reconstruct a TurnSummary instance from a previously saved dictionary representation (from_dict classmethod).

### Attributes

| Attribute | Type | Visibility | Description |
| --- | --- | --- | --- |
| `turn_number` (class) | `int` | 🔓 public | Class attribute turn_number |
| `objective` (class) | `str` | 🔓 public | Class attribute objective |
| `result_preview` (class) | `str` | 🔓 public | Class attribute result_preview |
| `timestamp` (class) | `str` | 🔓 public | Class attribute timestamp |
| `steps_used` (class) | `int` | 🔓 public | Class attribute steps_used<br>**Default:** `0` |
| `replay_seq_start` (class) | `int` | 🔓 public | Class attribute replay_seq_start<br>**Default:** `0` |

### Methods

#### Public Methods

| Method | Type | Description |
| --- | --- | --- |
| `to_dict` | instance | Method to_dict.<br>**Signature:** `def to_dict(self) -> dict[str, Any]:` |
| `from_dict` | classmethod | Method from_dict.<br>**Signature:** `def from_dict(cls, d: dict[str, Any]) -> 'TurnSummary':` |

---



#### to_dict

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def to_dict(self) -> dict[str, Any]:
```

### Description

Convert this TurnSummary instance into a plain Python dictionary representation suitable for serialization or inspection.

Convert this TurnSummary instance into a plain Python dictionary representation suitable for serialization or inspection. The method collects the instance's relevant attributes and returns them as key-value pairs in a dict.

### Returns

**Type:** `dict[str, Any]`

Returns a dict[str, Any]

### Complexity

Not analyzed

---



#### from_dict

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def from_dict(cls, d: dict[str, Any]) -> 'TurnSummary':
```

### Description

Constructs and returns a TurnSummary object from a dictionary representation, restoring the instance state from the provided mapping of attributes to values.

Constructs and returns a TurnSummary object from a dictionary representation, restoring the instance state from the provided mapping of attributes to values.


d is a dictionary containing the serialized fields of a TurnSummary (attribute names mapped to their values) used to reconstitute the object.

A TurnSummary instance populated with the data from the input dictionary.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `d` | `dict[str, Any]` | ✅ | Parameter d |

### Returns

**Type:** `'TurnSummary'`

A TurnSummary instance populated with the data from the input dictionary.

### Complexity

Not analyzed

---



#### RLMEngine

![Type: Concrete](https://img.shields.io/badge/Type-Concrete-green) ![Methods: 13](https://img.shields.io/badge/Methods-13-blue) ![Attributes: 13](https://img.shields.io/badge/Attributes-13-blue)

### Description

RLMEngine is a dataclass that orchestrates interaction between a language model, tools, and session state to solve tasks.

Within agent/engine.py, RLMEngine is the central execution engine that ties together model instances, tools, and configuration to implement the agent's runtime behavior. It acts as the primary component responsible for carrying out task solving, managing sessions, and coordinating lower-level model/tool interactions.

Responsibilities:
- Coordinate model invocations and tool usage to drive solving workflows (solve, solve_with_context, _solve_recursive).
- Manage session-specific state including tokens, directories, caching of models, and per-session bookkeeping (_model_cache, session_tokens, session_dir, session_id).
- Execute and evaluate tool calls and subtask results, including running single tool calls, applying tool outputs, and judging results against acceptance criteria (_run_one_tool, _apply_tool_call, _judge_result).
- Emit observations and enforce runtime policies and observation clipping while supporting cancelation and concurrency primitives (_emit, _clip_observation, _runtime_policy_check, cancel, _lock, _cancel).
- Provide artifact management for conversation and tool artifacts, including listing and reading stored artifacts (_list_artifacts, _read_artifact).

### Attributes

| Attribute | Type | Visibility | Description |
| --- | --- | --- | --- |
| `model` (class) | `BaseModel` | 🔓 public | Class attribute model |
| `tools` (class) | `WorkspaceTools` | 🔓 public | Class attribute tools |
| `config` (class) | `AgentConfig` | 🔓 public | Class attribute config |
| `system_prompt` (class) | `str` | 🔓 public | Class attribute system_prompt<br>**Default:** `''` |
| `session_tokens` (class) | `dict[str, dict[str, int]]` | 🔓 public | Class attribute session_tokens<br>**Default:** `field(default_factory=dict)` |
| `model_factory` (class) | `ModelFactory | None` | 🔓 public | Class attribute model_factory<br>**Default:** `None` |
| `_model_cache` (class) | `dict[tuple[str, str | None], BaseModel]` | 🔓 public | Class attribute _model_cache<br>**Default:** `field(default_factory=dict)` |
| `_lock` (class) | `threading.Lock` | 🔓 public | Class attribute _lock<br>**Default:** `field(default_factory=threading.Lock)` |
| `session_dir` (class) | `Path | None` | 🔓 public | Class attribute session_dir<br>**Default:** `None` |
| `session_id` (class) | `str | None` | 🔓 public | Class attribute session_id<br>**Default:** `None` |
| `_shell_command_counts` (class) | `dict[tuple[int, str], int]` | 🔓 public | Class attribute _shell_command_counts<br>**Default:** `field(default_factory=dict)` |
| `_cancel` (class) | `threading.Event` | 🔓 public | Class attribute _cancel<br>**Default:** `field(default_factory=threading.Event)` |
| `_pending_image` (class) | `threading.local` | 🔓 public | Class attribute _pending_image<br>**Default:** `field(default_factory=threading.local)` |

### Methods

#### Public Methods

| Method | Type | Description |
| --- | --- | --- |
| `__post_init__` | instance | Method __post_init__.<br>**Signature:** `def __post_init__(self) -> None:` |
| `cancel` | instance | Signal the engine to stop after the current model call or tool.<br>**Signature:** `def cancel(self) -> None:` |
| `solve` | instance | Method solve.<br>**Signature:** `def solve(self, objective: str, on_event: EventCallback | None = None) -> str:` |
| `solve_with_context` | instance | Method solve_with_context.<br>**Signature:** `def solve_with_context(self, objective: str, context: ExternalContext | None = None, on_event: EventCallback | None = None, on_step: StepCallback | None = None, on_content_delta: ContentDeltaCallback | None = None, replay_logger: ReplayLogger | None = None, turn_history: list[TurnSummary] | None = None) -> tuple[str, ExternalContext]:` |

#### Protected Methods

| Method | Type | Description |
| --- | --- | --- |
| `_emit` | instance | Method _emit.<br>**Signature:** `def _emit(self, msg: str, on_event: EventCallback | None) -> None:` |
| `_clip_observation` | instance | Method _clip_observation.<br>**Signature:** `def _clip_observation(self, text: str) -> str:` |
| `_runtime_policy_check` | instance | Method _runtime_policy_check.<br>**Signature:** `def _runtime_policy_check(self, name: str, args: dict[str, Any], depth: int) -> str | None:` |
| `_judge_result` | instance | Evaluate a subtask/execute result against acceptance criteria using a cheap judge model.<br>**Signature:** `def _judge_result(self, objective: str, acceptance_criteria: str, result: str, current_model: BaseModel | None = None) -> str:` |
| `_solve_recursive` | instance | Method _solve_recursive.<br>**Signature:** `def _solve_recursive(self, objective: str, depth: int, context: ExternalContext, on_event: EventCallback | None = None, on_step: StepCallback | None = None, on_content_delta: ContentDeltaCallback | None = None, deadline: float = 0, model_override: BaseModel | None = None, replay_logger: ReplayLogger | None = None, turn_history: list[TurnSummary] | None = None) -> str:` |
| `_run_one_tool` | instance | Run a single tool call. Returns (ToolResult, is_final).<br>**Signature:** `def _run_one_tool(self, tc: ToolCall, depth: int, step: int, objective: str, context: ExternalContext, on_event: EventCallback | None, on_step: StepCallback | None, deadline: float, current_model: BaseModel, replay_logger: ReplayLogger | None, parallel_group_id: str | None = None, parallel_owner: str | None = None) -> tuple[ToolResult, bool]:` |
| `_apply_tool_call` | instance | Method _apply_tool_call.<br>**Signature:** `def _apply_tool_call(self, tool_call: ToolCall, depth: int, context: ExternalContext, on_event: EventCallback | None, on_step: StepCallback | None, deadline: float = 0, current_model: BaseModel | None = None, replay_logger: ReplayLogger | None = None, step: int = 0) -> tuple[bool, str]:` |
| `_list_artifacts` | instance | List available artifacts.<br>**Signature:** `def _list_artifacts(self) -> str:` |
| `_read_artifact` | instance | Read an artifact's conversation log.<br>**Signature:** `def _read_artifact(self, artifact_id: str, offset: int = 0, limit: int = 100) -> str:` |

---



#### __post_init__

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def __post_init__(self) -> None:
```

### Description

Runs additional initialization steps immediately after the RLMEngine instance is created.

Runs additional initialization steps immediately after the RLMEngine instance is created. This method is intended to perform any setup that depends on fully-initialized instance attributes, such as validating configuration, initializing internal state or resources, and preparing the engine to be used.

### Complexity

Not analyzed

---



#### cancel

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def cancel(self) -> None:
```

### Description

Signals the engine to stop execution after the current model call or tool finishes.

Signals the engine to stop execution after the current model call or tool finishes. This sets a cancellation flag so the engine will not start any new model calls or tools and will exit at the next safe point.

### Complexity

Not analyzed

---



#### solve

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def solve(self, objective: str, on_event: EventCallback | None = None) -> str:
```

### Description

Attempts to solve the provided objective string and produce a string result representing the solution or outcome.

Attempts to solve the provided objective string and produce a string result representing the solution or outcome. It executes the engine's solving process using the given objective and any supplied event callback.


objective is the textual goal to be solved; on_event is an optional callback invoked with progress or event updates during the solving process.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `objective` | `str` | ✅ | Parameter objective |
| `on_event` = `None` | `EventCallback | None` | ❌ | Parameter on_event |

### Returns

**Type:** `str`

Returns a str

### Complexity

Not analyzed

---



#### solve_with_context

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def solve_with_context(self, objective: str, context: ExternalContext | None = None, on_event: EventCallback | None = None, on_step: StepCallback | None = None, on_content_delta: ContentDeltaCallback | None = None, replay_logger: ReplayLogger | None = None, turn_history: list[TurnSummary] | None = None) -> tuple[str, ExternalContext]:
```

### Description

Execute a problem-solving run for the given objective, using an optional external context and a set of callback hooks to report events, steps, and content updates.

Execute a problem-solving run for the given objective, using an optional external context and a set of callback hooks to report events, steps, and content updates. The method coordinates the engine's reasoning process (optionally replaying prior interactions) and returns a final textual result together with the (possibly updated) external context.


context supplies external information/state available to the solver; on_event, on_step, and on_content_delta are callback hooks invoked during the run to report lifecycle events, individual reasoning steps, and incremental content changes respectively; replay_logger and turn_history allow replaying or referencing prior interaction history to influence the current run.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `objective` | `str` | ✅ | Parameter objective |
| `context` = `None` | `ExternalContext | None` | ❌ | Parameter context |
| `on_event` = `None` | `EventCallback | None` | ❌ | Parameter on_event |
| `on_step` = `None` | `StepCallback | None` | ❌ | Parameter on_step |
| `on_content_delta` = `None` | `ContentDeltaCallback | None` | ❌ | Parameter on_content_delta |
| `replay_logger` = `None` | `ReplayLogger | None` | ❌ | Parameter replay_logger |
| `turn_history` = `None` | `list[TurnSummary] | None` | ❌ | Parameter turn_history |

### Returns

**Type:** `tuple[str, ExternalContext]`

Returns a tuple[str, ExternalContext]

### Complexity

Not analyzed

---



#### _emit

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _emit(self, msg: str, on_event: EventCallback | None) -> None:
```

### Description

Send or forward a textual message to an optional event callback.

Send or forward a textual message to an optional event callback. The method takes a message string and, if an EventCallback is supplied, invokes that callback to emit or publish the message to listeners or logging systems.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `msg` | `str` | ✅ | Parameter msg |
| `on_event` | `EventCallback | None` | ✅ | Parameter on_event |

### Complexity

Not analyzed

---



#### _clip_observation

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _clip_observation(self, text: str) -> str:
```

### Description

Takes an observation string and shortens or trims it to meet the engine's internal constraints or formatting requirements before further processing.

Takes an observation string and shortens or trims it to meet the engine's internal constraints or formatting requirements before further processing. This helper ensures the returned text is an appropriately clipped version of the input observation.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `text` | `str` | ✅ | Parameter text |

### Returns

**Type:** `str`

Returns a str

### Complexity

Not analyzed

---



#### _runtime_policy_check

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _runtime_policy_check(self, name: str, args: dict[str, Any], depth: int) -> str | None:
```

### Description

Performs a runtime policy check for an operation or item identified by name, using the provided argument mapping and the current recursion/operation depth.

Performs a runtime policy check for an operation or item identified by name, using the provided argument mapping and the current recursion/operation depth. If a policy violation or reason to block the operation is detected it returns a descriptive string, otherwise it returns None.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `str` | ✅ | Parameter name |
| `args` | `dict[str, Any]` | ✅ | Parameter args |
| `depth` | `int` | ✅ | Parameter depth |

### Returns

**Type:** `str | None`

Returns a str | None

### Complexity

Not analyzed

---



#### _judge_result

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _judge_result(self, objective: str, acceptance_criteria: str, result: str, current_model: BaseModel | None = None) -> str:
```

### Description

Evaluate a subtask or execution result against provided acceptance criteria by running a lightweight judge model to determine whether the result meets the objective.

Evaluate a subtask or execution result against provided acceptance criteria by running a lightweight judge model to determine whether the result meets the objective. The function performs a cheap automated judgement of the result in the context of the given objective and criteria.


objective, acceptance_criteria, and result are strings representing the task goal, the conditions that must be met, and the output to evaluate, respectively. current_model is an optional BaseModel instance to use as the judge; if not provided, a default lightweight judge model is used.

Returns a string containing the judge's evaluation or verdict about whether the result satisfies the acceptance criteria.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `objective` | `str` | ✅ | Parameter objective |
| `acceptance_criteria` | `str` | ✅ | Parameter acceptance_criteria |
| `result` | `str` | ✅ | Parameter result |
| `current_model` = `None` | `BaseModel | None` | ❌ | Parameter current_model |

### Returns

**Type:** `str`

Returns a string containing the judge's evaluation or verdict about whether the result satisfies the acceptance criteria.

### Complexity

Not analyzed

---



#### _solve_recursive

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _solve_recursive(self, objective: str, depth: int, context: ExternalContext, on_event: EventCallback | None = None, on_step: StepCallback | None = None, on_content_delta: ContentDeltaCallback | None = None, deadline: float = 0, model_override: BaseModel | None = None, replay_logger: ReplayLogger | None = None, turn_history: list[TurnSummary] | None = None) -> str:
```

### Description

Recursively attempts to solve or fulfill the given textual objective within a provided external context, potentially invoking callbacks for events, steps, and content updates during the recursive process.

Recursively attempts to solve or fulfill the given textual objective within a provided external context, potentially invoking callbacks for events, steps, and content updates during the recursive process. It supports depth-limited recursion, optional model selection, replay logging, and an optional history of prior turns to inform the solving process.


Key parameters: 'depth' limits recursion depth; 'context' provides external information or services the solver can use; 'on_event', 'on_step', and 'on_content_delta' are optional callbacks invoked during solving for observability or interaction; 'deadline' is a time cutoff; 'model_override' selects an alternate model for internal reasoning; 'replay_logger' records behavior for later playback; and 'turn_history' supplies previous turn summaries to influence decisions.

Returns a string representing the resulting solution, decision, or final output produced by the recursive solving process.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `objective` | `str` | ✅ | Parameter objective |
| `depth` | `int` | ✅ | Parameter depth |
| `context` | `ExternalContext` | ✅ | Parameter context |
| `on_event` = `None` | `EventCallback | None` | ❌ | Parameter on_event |
| `on_step` = `None` | `StepCallback | None` | ❌ | Parameter on_step |
| `on_content_delta` = `None` | `ContentDeltaCallback | None` | ❌ | Parameter on_content_delta |
| `deadline` = `0` | `float` | ❌ | Parameter deadline |
| `model_override` = `None` | `BaseModel | None` | ❌ | Parameter model_override |
| `replay_logger` = `None` | `ReplayLogger | None` | ❌ | Parameter replay_logger |
| `turn_history` = `None` | `list[TurnSummary] | None` | ❌ | Parameter turn_history |

### Returns

**Type:** `str`

Returns a string representing the resulting solution, decision, or final output produced by the recursive solving process.

### Complexity

Not analyzed

---



#### _run_one_tool

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _run_one_tool(self, tc: ToolCall, depth: int, step: int, objective: str, context: ExternalContext, on_event: EventCallback | None, on_step: StepCallback | None, deadline: float, current_model: BaseModel, replay_logger: ReplayLogger | None, parallel_group_id: str | None = None, parallel_owner: str | None = None) -> tuple[ToolResult, bool]:
```

### Description

Execute a single tool invocation (ToolCall) within the engine, handling any required context, callbacks, deadlines, model information, and optional parallel execution metadata.

Execute a single tool invocation (ToolCall) within the engine, handling any required context, callbacks, deadlines, model information, and optional parallel execution metadata. It returns the tool's result together with a boolean indicating whether that result should be treated as the final output for the current run.


on_event and on_step are optional callbacks invoked for lifecycle events and per-step notifications; deadline is a timestamp by which the tool run should complete; current_model provides the model instance to use for any model-driven behavior; replay_logger, if provided, records execution details for replay/debugging. parallel_group_id and parallel_owner are optional strings used to coordinate or attribute this call when running tools in parallel.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `tc` | `ToolCall` | ✅ | Parameter tc |
| `depth` | `int` | ✅ | Parameter depth |
| `step` | `int` | ✅ | Parameter step |
| `objective` | `str` | ✅ | Parameter objective |
| `context` | `ExternalContext` | ✅ | Parameter context |
| `on_event` | `EventCallback | None` | ✅ | Parameter on_event |
| `on_step` | `StepCallback | None` | ✅ | Parameter on_step |
| `deadline` | `float` | ✅ | Parameter deadline |
| `current_model` | `BaseModel` | ✅ | Parameter current_model |
| `replay_logger` | `ReplayLogger | None` | ✅ | Parameter replay_logger |
| `parallel_group_id` = `None` | `str | None` | ❌ | Parameter parallel_group_id |
| `parallel_owner` = `None` | `str | None` | ❌ | Parameter parallel_owner |

### Returns

**Type:** `tuple[ToolResult, bool]`

Returns a tuple[ToolResult, bool]

### Complexity

Not analyzed

---



#### _apply_tool_call

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _apply_tool_call(self, tool_call: ToolCall, depth: int, context: ExternalContext, on_event: EventCallback | None, on_step: StepCallback | None, deadline: float = 0, current_model: BaseModel | None = None, replay_logger: ReplayLogger | None = None, step: int = 0) -> tuple[bool, str]:
```

### Description

Execute or apply a given ToolCall within the engine, invoking the provided external context and any callbacks, handling recursion depth, deadlines, and optional replay logging.

Execute or apply a given ToolCall within the engine, invoking the provided external context and any callbacks, handling recursion depth, deadlines, and optional replay logging. It coordinates the interaction with an optional current model and reports the outcome of the tool invocation.


tool_call is the tool invocation to run; depth controls recursion/step nesting; context provides external services/environment for the tool; on_event and on_step are optional callbacks invoked as the call proceeds; deadline caps how long the operation may run; current_model supplies an optional model used for the call; replay_logger records events for later replay; step is the current step index.

Returns a tuple where the boolean indicates success/termination status and the string contains the tool's textual result or status message.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `tool_call` | `ToolCall` | ✅ | Parameter tool_call |
| `depth` | `int` | ✅ | Parameter depth |
| `context` | `ExternalContext` | ✅ | Parameter context |
| `on_event` | `EventCallback | None` | ✅ | Parameter on_event |
| `on_step` | `StepCallback | None` | ✅ | Parameter on_step |
| `deadline` = `0` | `float` | ❌ | Parameter deadline |
| `current_model` = `None` | `BaseModel | None` | ❌ | Parameter current_model |
| `replay_logger` = `None` | `ReplayLogger | None` | ❌ | Parameter replay_logger |
| `step` = `0` | `int` | ❌ | Parameter step |

### Returns

**Type:** `tuple[bool, str]`

Returns a tuple where the boolean indicates success/termination status and the string contains the tool's textual result or status message.

### Complexity

Not analyzed

---



#### _list_artifacts

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _list_artifacts(self) -> str:
```

### Description

Retrieves and returns a textual listing of the available artifacts managed by the engine.

Retrieves and returns a textual listing of the available artifacts managed by the engine. The method enumerates whatever artifacts the RLMEngine exposes and formats that information into a string for consumption or display.

### Returns

**Type:** `str`

Returns a str

### Complexity

Not analyzed

---



#### _read_artifact

![Type: Sync](https://img.shields.io/badge/Type-Sync-green)

### Signature

```python
def _read_artifact(self, artifact_id: str, offset: int = 0, limit: int = 100) -> str:
```

### Description

Read and retrieve the conversation log associated with a specific artifact.

Read and retrieve the conversation log associated with a specific artifact. The method fetches a portion of the artifact's log starting at a given offset and limited to a maximum number of entries or characters.

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `artifact_id` | `str` | ✅ | Parameter artifact_id |
| `offset` = `0` | `int` | ❌ | Parameter offset |
| `limit` = `100` | `int` | ❌ | Parameter limit |

### Returns

**Type:** `str`

Returns a str

### Complexity

Not analyzed

---


